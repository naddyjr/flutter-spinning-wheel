name: flutter_spinning_wheel
description: A customizable widget to use as a spinning wheel in Flutter.
version: 1.1.0
homepage: https://github.com/BrunoJurkovic/flip_card/


environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
